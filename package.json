{
  "name": "task-manager-monorepo",
  "version": "1.0.0",
  "description": "Personal Task Manager monorepo containing NestJS backend and Next.js frontend",
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "backend:start": "npm run start:dev --prefix backend",
    "backend:test": "npm run test --prefix backend",
    "frontend:start": "npm run dev --prefix frontend",
    "frontend:test": "npm run test --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "test": "npm run backend:test && npm run frontend:test"
  }
}
